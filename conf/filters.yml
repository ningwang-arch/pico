# only works on http server
filters:
  - name: "filter1"
    class: HelloFilter
    description: "Filter1 description"
    path:
      - /
      - /set
    init_params:
      param1: "value1"
      param2: "value2"
  - name: "filter2"
    path:
      - /
      - /get
    class: TestFilter
    description: "Filter2 description"
    init_params:
      param1: "value1"
      param2: "value2"
